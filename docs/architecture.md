# Architecture

The repository separates Vercel API functions, browser assets, generated public data, operational pipelines, and source documentation. Static browser URLs are mapped to `web/` with explicit Vercel rewrites; API and DB URLs remain independent.
